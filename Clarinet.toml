[project]
name = "enhance-conviction"
authors = ["Web3 Innovation Labs"]
description = "A smart contract platform for secure and compliant asset tokenization"
telemetry = false
requirements = []
[contracts.asset-conviction]
path = "contracts/asset-conviction.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
